'use client';

import LanguageSelector from '../components/LanguageSelector';

const Register = () => {
	return (
		<main className="flex flex-col items-center justify-center py-10">
			<LanguageSelector type="register" />
			언어:
		</main>
	);
};

export default Register;
