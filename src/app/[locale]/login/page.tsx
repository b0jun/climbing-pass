import Image from 'next/image';

const Login = () => {
	return (
		<section className="flex flex-col">
			<div className="flex flex-col px-5 mx-auto"></div>
		</section>
	);
};

export default Login;
